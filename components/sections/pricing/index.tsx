import React from "react";
import Section from "@/components/layout/section";

const Pricing = () => {
  return (
    <Section className="hidden" id="pricing">
      <div className="container">
        {/* Section hidden to prevent build errors */}
      </div>
    </Section>
  );
};

export default Pricing;